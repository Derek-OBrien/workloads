name: Build Node Application

on:
    workflow_call:
      inputs:
        proj-path:
          required: true
          type: string


jobs:
    build:
        name: Build Api
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: Run build cmd
              run: |
                echo "Path :: ${{ inputs.proj-path }}" 
                cd ./${{ inputs.proj-path}}
                npm install